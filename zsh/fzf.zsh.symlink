# Setup fzf
# ---------
if [[ ! "$PATH" == *$HOME/.dotfiles/contrib/fzf/bin* ]]; then
  export PATH="${PATH:+${PATH}:}$HOME/.dotfiles/contrib/fzf/bin"
fi

# Auto-completion
# ---------------
[[ $- == *i* ]] && source "$HOME/.dotfiles/contrib/fzf/shell/completion.zsh" 2> /dev/null

# Key bindings
# ------------
source "$HOME/.dotfiles/contrib/fzf/shell/key-bindings.zsh"
